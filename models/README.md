# Model artifacts

Run the training notebook to regenerate the classifier and TF-IDF vectorizer. Old artifacts were removed because their training procedure oversampled before the train/test split. Pickle files are environment-dependent and should only be loaded from trusted sources.
